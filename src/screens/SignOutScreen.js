import React from "react";
import { StyleSheet, Text, View } from "react-native";

export function SignOutScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <Text>This is SignOutScreen</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
